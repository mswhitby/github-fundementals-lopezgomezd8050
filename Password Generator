import random
import string

chars=string.ascii_letters+string.digits+string.punctuation

print("Password Generator")
length=int(input("How many characters do you want the passwords to be?"))

while length>0:
  print(random.choice(chars),end="")
  length=length-1
